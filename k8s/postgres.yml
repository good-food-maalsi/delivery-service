apiVersion: apps/v1
kind: StatefulSet
metadata:
    name: delivery-postgres
    labels:
        app: delivery-postgres
spec:
    serviceName: delivery-postgres
    replicas: 1
    selector:
        matchLabels:
            app: delivery-postgres
    template:
        metadata:
            labels:
                app: delivery-postgres
        spec:
            containers:
                - name: postgres
                  image: postgres:15-alpine
                  ports:
                      - containerPort: 5432
                  env:
                      - name: POSTGRES_USER
                        valueFrom:
                            secretKeyRef:
                                name: delivery-service-db-secret
                                key: POSTGRES_USER
                      - name: POSTGRES_PASSWORD
                        valueFrom:
                            secretKeyRef:
                                name: delivery-service-db-secret
                                key: POSTGRES_PASSWORD
                      - name: POSTGRES_DB
                        valueFrom:
                            secretKeyRef:
                                name: delivery-service-db-secret
                                key: POSTGRES_DB
                  volumeMounts:
                      - name: postgres-data
                        mountPath: /var/lib/postgresql/data
                  resources:
                      requests:
                          memory: "128Mi"
                          cpu: "100m"
                      limits:
                          memory: "512Mi"
                          cpu: "500m"
    volumeClaimTemplates:
        - metadata:
              name: postgres-data
          spec:
              accessModes: ["ReadWriteOnce"]
              resources:
                  requests:
                      storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
    name: delivery-postgres
spec:
    selector:
        app: delivery-postgres
    ports:
        - protocol: TCP
          port: 5432
          targetPort: 5432
